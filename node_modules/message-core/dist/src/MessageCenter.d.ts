declare class MessageCenter {
    private dict;
    private eVec;
    private lastRunTime;
    constructor();
    /**
     * 添加消息监听
     * @param type 消息唯一标识
     * @param listener 侦听函数
     * @param listenerObj 侦听函数所属对象
     *
     */
    listen(type: string, listener: Function, listenerObj: any): void;
    /**
     * 移除消息监听
     * @param type 消息唯一标识
     * @param listener 侦听函数
     * @param listenerObj 侦听函数所属对象
     */
    unlisten(type: string, listener: Function, listenerObj: any): void;
    /**
     * 触发消息
     * @param type 消息唯一标识
     * @param param 消息参数
     *
     */
    dispatch(type: string, ...param: any[]): void;
    /**
     * 运行
     *
     */
    run(): void;
    /**
     * 处理一条消息
     * @param msgVo
     */
    private dealMsg;
}
export declare const messageCenter: MessageCenter;
export {};
